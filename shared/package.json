{
  "name": "@plastic-crack/shared",
  "version": "1.0.0",
  "description": "Shared types, utilities, and configurations for Plastic Crack",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests specified for shared package\" && exit 0",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "eslint": "^9.17.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
