# Environment Configuration
NODE_ENV=development
PORT=3001

# API Configuration
API_VERSION=v1
API_BASE_URL=/api

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=debug

# Database (to be configured later)
# DATABASE_URL=postgresql://user:password@localhost:5432/plastic_crack

# Redis (to be configured later)
# REDIS_URL=redis://localhost:6379

# JWT (to be configured later)
# JWT_SECRET=your-super-secret-jwt-key
# JWT_EXPIRES_IN=7d

# AWS S3 (to be configured later)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# S3_BUCKET_NAME=
